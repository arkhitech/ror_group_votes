<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user_group.name %>
</p>

<p>
  <strong>Owner:</strong>
  <%= User.find(@user_group.user_id).name %>
</p>

<p>
   
  <strong><%= link_to 'Invite User to Group', new_invite_path %></strong>

</p>
<%= link_to 'Edit', edit_user_group_path(@user_group) %> |
<%= link_to 'Back', user_groups_path %>
